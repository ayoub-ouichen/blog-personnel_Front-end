import { DateAsAgoPipe } from './date-as-ago-pipe.pipe';

describe('DateAsAgoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateAsAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
